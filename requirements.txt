# WiseTravel demo
# Phase 1 (pipeline dữ liệu địa điểm)
requests>=2.31
pandas>=2.0
folium>=0.15
streamlit>=1.30
tabulate>=0.9          # pandas .to_markdown()

# Phase 2 (cảm xúc) — NẶNG: torch tải vài trăm MB.
#   Chỉ cần khi chạy 09_eval_models.py / 10_aggregate_reviews.py.
transformers>=4.40
torch>=2.2
sentencepiece>=0.1.99  # tokenizer ViSoBERT/PhoBERT (slow tokenizer)
scikit-learn>=1.3
pyvi>=0.1.1            # tách từ tiếng Việt cho PhoBERT
matplotlib>=3.7
datasets>=2.18         # kéo review tiếng Việt từ HuggingFace (seed tập gold)

# Phase 3 (planner — tùy chọn) sẽ bổ sung khi tới phase đó.
